name: Eternal Seal Check
on: [push, pull_request]
jobs:
  seal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify living canon exists
        run: test -f canon/living.md || (echo "CANON MISSING — SEAL BREACH" && exit 1)
